//it('adds event listener correctly', () => {
  //let event_target;
  //let target = component.querySelector('.test');
  //let handler = chai.spy((event, target) => {
    //event_target = target;
  //});
  //let handler2 = chai.spy(() => {});
  //let handler3 = chai.spy(() => {});
  //component.on('test-event-delegation', '.test', handler);
  //component.on('test-event-delegation', '.test2', handler2);
  //component.on('test-event-delegation', handler3);
  //fireEvent('test-event-delegation', target);
  //expect(handler).to.have.been.called.once;
  //assert.equal(event_target, target);
  //expect(handler2).not.to.have.been.called.once;
  //expect(handler3).to.have.been.called.once;
//});

//it('removes event listener correctly', () => {
  //let target = component.querySelector('.test');
  //let handler = chai.spy(() => {});
  //component.on('test-event-delegation', handler);
  //fireEvent('test-event-delegation', target);
  //expect(handler).to.have.been.called.once;
  //component.off('test-event-delegation', handler);
  //fireEvent('test-event-delegation', target);
  //expect(handler).to.have.been.called.once;
//});
